import * as React from 'react';
export const WindowContext = React.createContext(undefined);
export function WindowContextProvider(props) {
    const windowContextRef = React.useRef(typeof window !== 'undefined' ? window : undefined);
    return (React.createElement(WindowContext.Provider, { value: windowContextRef.current }, props.children));
}
export function useWindow() {
    return React.useContext(WindowContext);
}
