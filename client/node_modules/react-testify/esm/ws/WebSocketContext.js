import * as React from 'react';
export const WebSocketContext = React.createContext({});
export function WebSocketContextProvider(props) {
    const possibleWindow = (typeof global !== 'undefined' ? window : {});
    return (React.createElement(WebSocketContext.Provider, { value: possibleWindow.WebSocket }, props.children));
}
